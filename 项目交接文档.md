# 项目交接文档（dealoptimizer 线下凑单 App）

> **用途**：新会话直接读此文档即可完整接手，无需翻任何其他交接/需求文档。  
> **文档状态**：反映 2026-07-12 深夜实际代码（含本轮代码审查 4🔴+4🟡 修复、versionCode 自动递增）。  
> **项目根**：`C:\Users\10516\Documents\deepseek_work\test2-main`  
> **包名**：`com.example.dealoptimizer`  
> **分支**：`feature/product-list-ui-redesign`（本地领先 origin 2 个 commit 未 push）

---

## 0. 项目定位

线下服装购物的「凑单 + 优惠券最优组合」计算器。用户录入**商品、必买标记、归属用户、优惠券规则**，App 算出**最省钱的实付方案**并支持多人拼单分摊。

- 解决的真实问题：满减门槛凑单、多券组合（叠加 / 单用）、多人拼单分摊。
- 线下场景：手动录入，不爬电商 API。
- 三个 Tab（`MainScreen`）：**商品 / 优惠券 / 计算**。用户管理不新增 Tab，内嵌商品页左上角 Drawer。
- 原始需求背景见 `需求分析.md`（早期草稿，数据模型已与实际不符，**以本文档为准**）。

---

## 1. 技术栈与编译环境

| 项 | 内容 |
|---|---|
| 语言 / 构建 | Kotlin + AGP 8.5 + Gradle（Hilt 2.51 / KSP）+ Compose BOM **1.5.4**（非 Material3、非 1.6+） |
| UI | Jetpack Compose **Material 1.5.4** + Navigation Compose |
| 存储 | Room（`AppDatabase`，当前 `version = 7`，entities：Product / Coupon / FillProduct / User） |
| DI | Hilt |
| 测试 | JUnit（`DiscountCalculatorTest`，13 个用例全绿） |

**编译 / 环境坑（必看）**：
- `JAVA_HOME` 必须设，指向 Android Studio 自带 JBR：本机 `C:\Program Files\Android\Android Studio\jbr`。
- 离线编译验证（沙箱用）：`export JAVA_HOME="..." && ./gradlew compileDebugKotlin --offline`，目标 **BUILD SUCCESSFUL 零 warning**。
- 沙箱 AAPT2 限制（本地不受影响）：`--rerun-tasks` / 全量 `assemble` 会在 `processDebugResources` 报 UCRT 缺失，与代码无关；验证 Kotlin 正确性用 `compileDebugKotlin --offline` 即可。
- **单测**：`./gradlew testDebugUnitTest --offline`。
- **versionCode 自动递增**：`app/build.gradle` 的 `defaultConfig` 用构建时间戳（分钟级）自动算 `versionCode`，避免手动拷贝 APK 覆盖安装时弹「已安装/替换」。上架发版前改回手填固定值。

**数据库迁移安全（本轮修过的雷区，务必遵守）**：
- `HiltModule` 的 `databaseBuilder` **已移除** `fallbackToDestructiveMigration()` 与 `allowMainThreadQueries()`（前者会静默清库、后者会 ANR）。现注册 `addMigrations(MIGRATION_3_4, 4_5, 5_6, 6_7)`，链路完整到 v7。
- `AppDatabase` `exportSchema = true`，并在 `app/build.gradle` 顶层配置 `ksp { arg("room.schemaLocation", "$projectDir/schemas") }`，编译生成 `app/schemas/.../7.json` 作迁移校验基准。
- **每次 DB 升版本必须配套写对应 `MIGRATION_x_y`**，否则缺迁移会抛异常导致 App 打不开（当前为首发版、几乎无老库用户，风险低）。
- `MIGRATION_5_6` 自增 INSERT 默认用户 + 校正 `sqlite_sequence`，**禁止指定 id 的自增 INSERT**（否则主键冲突崩溃）。
- 死代码 `di/AppContainer.kt` 已删除（全仓零引用）。

---

## 2. 功能清单

### 2.1 商品（`ProductScreen` + `UserProductSection`）
- 增删改、必买标记、归属用户、颜色标签（`color` 字段逗号分隔存库，v6→7 迁移加列）。
- 列表：用户分区 → 按四大类分组上色 → **3 列网格**。
  - 分类依据 `resolveCategory()`：商品名去掉颜色前缀后按 `upperClothingOptions` 等列表前缀匹配（短袖/长袖都归上半身）。
  - 类型上色（马卡龙浅色）：上半身浅蓝 / 下半身浅绿 / 全身浅黄 / 饰品浅紫；必买红底覆盖类型色（优先级最高）。
- 小卡片 `ProductMiniCard`：名 + 价 + 必买 `Checkbox`（替代原 Switch，避免吞拖拽）；`Ellipsis` 防溢出。
- 左滑删除：手势挂**前景 Card**（`draggable` + `clickable` 共存），底层红底删除区**点图标才删**（防误删）；两个方向都能滑。
- 左上角 Drawer 用户列表：`ModalDrawer` `gesturesEnabled = false` 防误触，需显式 X 关闭 + 菜单 toggle。

### 2.2 优惠券（`CouponScreen`）
- 三类：满减 `FULL_REDUCTION` / 折扣 `DISCOUNT` / 无门槛 `NO_THRESHOLD`。
- 规则：`isStackable`（叠加 / 单用）、`isEnabled` 启用停用、`maxUsages` 次数限制。
- 券模式切换：顶部 Segmented 控件，持久化 SharedPreferences（`CouponModeRepository.isStackableMode`）。计算时只启用当前模式券（两类不混算，UI 过滤）。

### 2.3 计算（`CalculatorScreen`）
- 最优分组、凑单小物（净收益判定才采用）、门槛提示（必买红 / ≤20 橙 / ≤40 绿）。
- 多选商品参与（必买始终保留），按用户分框。
- 多用户 Tab：**汇总**（`SummaryCard` 蓝底 + 每人应付）/ **分摊说明**（叠加券 `PerUserShareCard` / 单用券 `GroupCombinationCard` + `RemainingProductsCard`）。
- 单用户：`SolutionCard` + `FillSuggestionCard`。

### 2.4 多用户凑单
- 用户抽屉管理（添加 / 改名 / 删除 / 勾选参与，默认「本人」不可删，勾选持久化）。
- 两口径对比：单独算 vs 合并凑单分摊后（公平对比卡 `CandidateCompareCard`：现状两阶段 vs 全局 `multiUserMode=false + FAIR`）。
- 跨用户合并分摊（叠加券余量占比 / 单用券原价占比）。

---

## 3. 核心算法（`domain/algorithm/DiscountCalculator.kt`）

入口：单用户 / 合并解走 `calculateBestCombination(products, coupons, fillProducts, useFillProducts, multiUserMode, fairness)`。券先按 `isStackable == 当前模式` 过滤（一次仅一类券参与），再分流：
- `stackableCoupons` → `calculateStackableSolution`（每张独立整量算）
- `nonStackableCoupons` → `calculateNonStackableSolution`（分组背包）

### 3.1 叠加券（`isStackable = true`）
每张券基于**商品总价独立**算 `count = min(floor(totalAmount / threshold), usageLimit)`：
- 满减：`count` 次叠加，discount = `count × 面额`（总价 300 / 满 100 减 20 → count 3 减 60）。
- 折扣：达阈值 count=1，discount = 总价 × (折扣值 / 100)。
- 无门槛：count=1，discount = min(面额, 总价)。
- 多张叠加券各自算后累加 `totalDiscount`；`finalPrice = max(总价 − 总优惠, 0)`。
- **无分组、无凑单小物参与**（叠加券路径不调 `findBestExactGroups`）。

### 3.2 单用券（`isStackable = false`）
分组背包：每组配一张券，求总优惠最大的分组方案。
- **≤16 件精确解 `findBestExactGroups`**：
  1. `buildCandidates`：按券枚举所有「够门槛」的商品子集（bitmask 表示），带限界剪枝 DFS；凑单小物开启时补差价（gap ≤ 40），仅当 `discount > fillTotal`（净收益为正）才采用。
  2. `solve(availableMask, usagesLeft)`：记忆化递归，从剩余商品挑**不重叠**候选组，求总 `netSavings` 最大。
  3. `isBetterThan` 平局：netSavings > fillTotal > discount > 组数 > groupedTotal；单用户 `fairness = NONE`。
- **>16 件贪心解 `findGreedyGroups`**：商品升序，反复取面额最大券塞满门槛，牺牲最优性换性能；**已透传 `fairness` 参数**（`FairnessStrategy`），保证与精确路径行为边界一致。

### 3.3 多用户分摊（`CalculatorViewModel.calculateMultiUser`）
- `scoped` = 勾选商品 ∩ 勾选用户名下商品（必买始终保留）。
- `perUser` = 每人单独算（`multiUserMode = false`）。
- 两阶段分组：阶段1 每人自达标锁定单人组；阶段2 剩余跨用户凑单（`crossPool`, `multiUserMode = true`）。
- 分摊口径：
  - **叠加券**：增量 = `combined.totalDiscount − ΣperUser.totalDiscount`；按**余量占比**（`ownOriginal − 已用券门槛`）分摊增量。
  - **单用券**：逐组聚合，按**原价占比**分摊，凑单小物金额并入 `ownOriginal` 保证 `原价 − 优惠 = 应付`。
- 公平性 `FairnessStrategy`：仅总省钱平局时改优惠归属，不牺牲总省钱；「全局」候选必须用 `multiUserMode = false + FAIR`。

### 3.4 折扣计算的唯一真相源（本轮修复 #6）
- 所有折扣金额统一以 `CouponUsage.discount: Double` 字段为准（计算器在构造 `CouponUsage` 时写入 `discount = group.discount`）。
- `BundleSolution.getDiscountBreakdown()`、ViewModel 的 `usageDiscount()` 助手均**只读 `usage.discount`**，不再手抄 when 公式，修正了原 DISCOUNT 误用整包 originalTotal 的偏差。

### 3.5 本轮修复：叠加券合并凑单算错（commit `b3235ee`）
- **根因**：`combined` 原用「各人单独用券 + 空 crossPool」拼，纯叠加券下 crossPool 空 → combined 优惠恒等于各人之和 → `incrementalDiscount` 恒 0、合并价偏高、人均分摊增量 0。
- **修复**：叠加券分支 `combined` 直接 `calculateBestCombination(scoped)` 对**合并总价整量重算**（`count = floor(合并总价 / 阈值)`）；单用券路径不变。

---

## 4. 数据模型

- `User(id, nickname, isDefault, isSelected)` — `ownerId=1` 为本人。
- `Product(id, name, originalPrice, isRequired, ownerId=1, color="")` — color 逗号分隔。
- `Coupon(id, name, type, threshold, discountValue, maxUsages, isStackable, isEnabled, ...)`。
- `FillProduct(id, name, price, category, isHot)`。
- `BundleSolution(products, couponUsages, originalTotal, finalPrice{get} = originalTotal − 总优惠, fillProducts)`。
- `CouponUsage(coupon, count, productGroup, fillProducts, discount=0.0)` — **`discount` 为折扣唯一真相源**。
- `MergedShare(ownOriginal, remaining, discount, payable)`。

> 迁移：`AppDatabase` v6→7（`MIGRATION_6_7` 加 `products.color` 列）；`MIGRATION_5_6` 自增 INSERT 默认用户 + 校正 `sqlite_sequence`，**禁止指定 id 的自增 INSERT**。

---

## 5. 代码结构 & 关键锚点

`app/src/main/kotlin/com/example/dealoptimizer/`
- `data/model` · `data/database`(`AppDatabase` v7, exportSchema=true) · `data/dao` · `data/repository`(`CouponModeRepository`)
- `di/HiltModule`（databaseBuilder + addMigrations(3_4,4_5,5_6,6_7)，无 fallback / 无 allowMainThread）
- `domain/algorithm/DiscountCalculator.kt` — 全部算法
- `presentation/viewmodel/CalculatorViewModel.kt` — `calculateMultiUser`（多用户状态机）、`calculateForProducts`、`calculateBestPrice`（⚠ **死代码无调用方**）、`_error` StateFlow（异常保护）
- `presentation/ui/` — `ProductScreen.kt`（列表 + Drawer + 编辑弹窗）、`UserProductSection.kt`（用户分区 + 小卡片）、`CalculatorScreen.kt`（计算页全部卡片）、`CouponScreen.kt`（券模式切换）、`UserDrawer.kt`

关键符号（行号随改动漂移，以符号名为准）：
- `calculateMultiUser` / 两阶段分组 / shares 聚合 — `CalculatorViewModel.kt`
- `calculateBestCombination(... multiUserMode, fairness)` — `DiscountCalculator.kt`
- `buildCandidates` 中 owner 约束（`multiUserMode` 时 ≥2 owner 且任一已达标则剔除候选）— `DiscountCalculator.kt`
- `ProductSelectionPanel` + 全选 Checkbox — `CalculatorScreen.kt`
- `SummaryCard` / `PerUserShareCard` / `GroupCombinationCard` / `RemainingProductsCard` / `CandidateCompareCard` — `CalculatorScreen.kt`

---

## 6. 业务锁定规则（用户逐条拍板，不可擅自改）

| # | 规则 | 说明 |
|---|---|---|
| R1 | 单用券分摊 = 原价占比 | 凑单组内每人按「本人商品原价 / 组内商品原价」分摊优惠 |
| R2 | 叠加券分摊 = 余量占比 | 增量优惠按「原价 − 已用券门槛」剩余金额占比分摊 |
| R3 | 单人已达标 → 标「单人组·自达标」 | 自己小计 ≥ 门槛，单独用券，不进凑单组 |
| R4 | 真凑单组判定 | ≥2 不同 owner 且每人各自小计 < 门槛且组总价 ≥ 门槛；任一 owner 已达标则剔除 |
| R5 | 先自组，后凑单（两阶段） | 绝不允许把已达标大额商品拆去凑单（曾导致 49+59 同人误入组） |
| R6 | 分摊优惠 -¥XX 红 + 粗 | `AppRed` + Bold 13sp |
| R7 | 分组卡片标签 | 单人组蓝 / 凑单组绿 / 未达标橙灰 |
| R8 | 必买商品标红 | `isRequired` 计算页名称/价格 `AppRed`，优先级最高 |
| R9 | 门槛提示橙绿 | 距门槛 ≤20 橙 `AppAmber`，≤40 绿 `AppGreen`（无红黄） |
| R10 | 汇总卡蓝底白字 | `SummaryCard` 蓝底，列出每人应付行 |
| R11 | 券模式互斥 | 计算只启用当前模式券，两类不混算 |

---

## 7. 关键设计决策 & 雷区（必看）

1. **Compose 手势坑**：`swipeable` 在 1.5.4 仍在 `material` 包；手势挂**前景 Card** 不挂外层 Box；`Switch` → `Checkbox`（避免吞拖拽）；左滑删除防误删。
2. **Drawer 误触**：`gesturesEnabled = false` 后遮罩点击关闭失效，必须有 X 关闭 + 菜单 toggle。
3. **叠加券合并必须整量重算**（§3.5），不能拼各人之和。
4. **公平性 = 纯 tie-break**，永不因公平少省钱；「全局」候选必须 `multiUserMode = false`。
5. **单用券 shares 必须含凑单小物金额**，否则 `原价 − 优惠 ≠ 应付`。
6. **余量占比用 `原价 − 已用券门槛`**（非单独算应付）。
7. **迁移保数据**：`MIGRATION_5_6` 自增 INSERT + 校正 `sqlite_sequence`，禁止指定 id。
8. **红色专用于必买**：`thresholdGapColor` 不得返回 `AppRed`。
9. **Compose 限制**：`remember` 不能置 `LazyListScope` content lambda 顶层；`Modifier.weight` 是 `RowScope` 扩展。
10. **删文字 ≠ 删结构**：类型分组只删标题文字、保留分段布局。
11. **不确定就问，别乱动手**（用户原话）。

---

## 8. 本轮（代码审查）已落地修复

分支 `feature/product-list-ui-redesign`，均经编译/单测验证：

**🔴 阻断项（4 项，消除数据丢失与 ANR 风险）**
- 删 `HiltModule` 的 `allowMainThreadQueries()` + `fallbackToDestructiveMigration()`。
- 删死代码 `di/AppContainer.kt`（全仓零引用，连带消除 lateinit 隐患）。
- `AppDatabase` `exportSchema = false` → `true`；`app/build.gradle` 顶层加 `ksp { arg("room.schemaLocation", ...) }`，生成 `schemas/.../7.json` 迁移基准。

**🟡 建议项（4 项）**
- #5 贪心分支公平性透传（`findGreedyGroups` 加 `fairness` 形参，>16 件与精确路径行为一致）。
- #6 折扣统一为单一真相源（`CouponUsage.discount` 字段 + 计算器写入 + 视图层只读），修正 `BundleSolution` 漏算 fill 偏差。
- #7 关键路径异常保护（`CalculatorViewModel` 新增 `_error` StateFlow + `clearError()`，三个计算 launch 包 try/catch）。
- #9 补 2 个贪心路径单测（17 件触发贪心 + FAIR 不崩溃），单测总数 11→13 全绿。

**其他**
- `build.gradle` `defaultConfig` versionCode 基于构建时间戳自动递增（避免覆盖安装弹替换窗）。
- `.gitignore` 新增 `.workbuddy/` 忽略本地记忆目录。

> #8 货币精度（Double→Long 分）属**高风险全局重构，判定为延期项**，设计稿见 `货币精度改造设计文档.md`，未擅自动代码。

---

## 9. Git & 提交

- 分支 `feature/product-list-ui-redesign`（已跟踪 origin）。
- **本地领先 origin 2 个 commit 未 push**：`ffc3eea`（货币精度设计文档）、`ad01631`（versionCode 自动递增）。其余历史均已 push。
- `.workbuddy/` 不入库（本地工作记忆）。
- 提交习惯：改动确认后**指定文件** `git add`（避免 `git add .` 误带 `.workbuddy`）→ commit → push。

---

## 10. 用户偏好 & 协作约定（给新会话）

- 全程**中文**；风格高效务实、不灌水、不客套。
- 给方案多给**可比选项并排**（如「现状 vs 全局」双卡），让用户选，不替他拍板。
- 涉及正确性 / 方向问题**直接反对并给更优路**，不附和。
- 设计迭代靠截图确认，「ok 落地吧」后才改代码；改前先读现有代码与约定。
- 必选红色优先级不变；浅色背景保证黑字可读；改动要么不动、要么问清 95% 把握再动手。

---

## 11. 暂存项 & 待办（用户决定暂不实现）

| 项 | 说明 | 若实现需 |
|---|---|---|
| 手动拖拽分组篮子 | 计算页把商品拖入不同篮子（每篮 = 一次券使用），覆盖自动分组 | `pointerInput` + `onGloballyPositioned` 跨容器命中（零依赖，代码量大） |
| 分组持久化落库 | 手动分组存库，重启 / 换设备保留 | `AppDatabase` v7→8 迁移 + `BasketAssignment` 实体 / DAO / Repository + Hilt |
| 货币精度改造 | `Double` 金额 → Long 分 / BigDecimal | 见 `货币精度改造设计文档.md`（需先钉死测试基准再动） |

**已知局限**：
- >16 件商品走贪心路径，owner 约束轻量，极端组合可能非最优（仅影响大单）。
- 混合券模式（叠加 + 单用同时参与）算法层已支持，但 UI 当前按 `couponMode` 过滤只启用一类，未开放混算开关。

---

## 12. 其他有效文档索引（保留）

- `README.md` — 仓库首页概览、安装运行说明。
- `需求分析.md` — 早期需求草稿（**数据模型已过时，仅作背景参考，以本文档为准**）。
- `代码审查标准与流程.md` — 本项目代码审查标准（🔴/🟡/💭 分级）、流程、清理 Backlog。
- `货币精度改造设计文档.md` — #8 货币精度改造方案 + 测试基准（未来重构依据）。
